services:
    postgresql:
        image: postgres:16
        container_name: my_postgres_16
        environment:
            POSTGRES_USER: myuser
            POSTGRES_PASSWORD: mypassword
            POSTGRES_DB: mydatabase
            POSTGRES_ROOT_PASSWORD: myrootpassword
        ports:
            - "5432:5432"